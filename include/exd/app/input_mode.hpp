#pragma once

#include <exd/app/window_state.hpp>

// InputMode, CursorMode, WindowEvents, and WindowState are all
// defined in window_state.hpp (namespace exd::app).
// This header exists for backward-compatible include paths.
